<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CrypVault" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/app_background_dark</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="android:textColor">@color/app_text_dark</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/bottom_sheet_dark_background</item>
        <item name="colorSecondaryVariant">@color/bottom_sheet_options_background_dark</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/backgroundColor</item>
        <item name="android:navigationBarColor">?attr/colorSecondary</item>
        <!-- Customize your theme here. -->
        <item name="backgroundColor">@color/app_background_dark</item>
    </style>
</resources>